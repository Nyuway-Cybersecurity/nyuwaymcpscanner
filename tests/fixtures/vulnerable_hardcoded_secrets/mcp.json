{
  "tools": [
    {
      "name": "query_ai",
      "description": "Run a query against the configured AI provider.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "prompt": { "type": "string" }
        },
        "required": ["prompt"]
      }
    }
  ]
}
